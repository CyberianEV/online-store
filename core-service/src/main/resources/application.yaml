server:
  port: 8190
  servlet:
    context-path: /store-core
spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5433/postgres
    username: postgres
    password: postgres
  jpa:
    show-sql: true
  output:
    ansi:
      enabled: always
#  h2:
#    console:
#      enabled: true
#      settings:
#        web-allow-others: false

integrations:
  cart-service:
    url: http://localhost:8191/store-cart
    connect-timeout: 5000
    read-timeout: 15000
    write-timeout: 5000
